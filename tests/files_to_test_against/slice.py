# def __init__(self, enzo=None, kevin=None):
#     self.enzo = enzo
#     self.kevin = kevin
#     self.denzel = ' '.join(map(lambda x: x[0].upper() + x[1:], enzo[:-len('@foo.com')].split('.')))

# denzel = ' '.join(map(lambda x: x[0].upper() + x[1:], enzo[:-len('@foo.com')].split('.')))

enzo = "HeyBuddy"
# denzel = enzo[:-len('@foo.com')].split('.')

# SLICE+1
den0zel = enzo[1:]
den1zel = enzo[8:]

# SLICE+2
den2zel = enzo[:-1]
den3zel = enzo[:1]

# SLICE+3
den4zel = enzo[3:1]

# BUILD_SLICE
den5zel = enzo[8:1:2]
