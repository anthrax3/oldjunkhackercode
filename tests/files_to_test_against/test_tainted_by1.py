def picky(strange):
    c.next = request.params.get('next')
    c.tboo = strange
    the2nd_assign = c.tboo

    abby = abby.pie()
    if abby or not something(1) and c.tboo or c.next:
        self.redirect(c.next)
    # Does this have 1 thread or 2?
    return self.redirect(the2nd_assign)
